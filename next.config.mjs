/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pravatar.cc",
      "images.unsplash.com",
      "images.pexels.com",
      "freepnglogo.com",
      "pngset.com",
      "iconduck.com",
      "images.remotePatterns",
    ],
  },
};

export default nextConfig;
